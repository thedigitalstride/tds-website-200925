# Style Guide Access Control
#
# The /style-guide route is automatically blocked in production deployments
# but accessible in:
# - Local development (NODE_ENV=development)
# - Preview deployments (VERCEL_ENV=preview)
#
# To override and enable in production (not recommended), set:
# ENABLE_STYLE_GUIDE_IN_PRODUCTION=true
